# Server-side URL of the existing Express API (include /api).
API_URL=http://localhost:5000/api
# Public frontend origin for canonical URLs, social metadata and sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional legacy fallback when API_URL is not configured.
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
